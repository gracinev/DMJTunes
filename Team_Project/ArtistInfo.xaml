<Page
    x:Class="Team_Project.ArtistInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Team_Project"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <HyperlinkButton Click="HyperlinkButton_Click"  Content="Back"/>
            <TextBlock Text="{x:Bind ActiveArtist.Name}" Margin="40,40,40,0" HorizontalAlignment="Center" FontSize="30"  />
            <TextBlock Text="{x:Bind ActiveArtist.AlbumName}" Margin="40,40,40,0" HorizontalAlignment="Center" FontSize="30"  />
            <TextBlock Text="{x:Bind ActiveArtist.YearOnly}"  HorizontalAlignment="Center" Margin="5" />
            <TextBlock Margin="20" Text="{x:Bind ActiveArtist.Description}" />
            <Image Width="100" Height="120" Source="{x:Bind ActiveArtist.Image.Source}"/>
        </StackPanel>
    </Grid>
</Page>
